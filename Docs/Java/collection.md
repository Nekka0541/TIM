# コレクションとは

データをどのようにまとめて扱うかを##データ構造(data structure)##という

Javaには様々なデータ構造に対応したデータをまとめて格納するための入れ物クラスタたちがAPIとして準備されている。

これらを###コレクションフレームワーク###という

- コレクションにはできないこと
	- インスタンスでないものは格納できない
	- つまり基本データ型の情報を得て格納することができない。
	- だが、int型の情報をIntegerインスタンスに変換すれば格納が可能になる。

```java

```